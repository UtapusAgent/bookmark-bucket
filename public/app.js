const $=s=>document.querySelector(s);let items=[];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(p,o){const r=await fetch(p,{headers:{'content-type':'application/json'},...o});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||r.statusText);return d}async function load(){items=await api('/api/items');render()}async function addItem(x){await api('/api/items',{method:'POST',body:JSON.stringify(x)});await load()}async function deleteItem(id){if(confirm('Delete?')){await api('/api/items/'+id,{method:'DELETE'});await load()}}function shell(b){document.body.innerHTML='<main><header class="top"><div><h1>'+esc(APP.name)+'</h1><p class="muted">'+esc(APP.desc)+'</p></div></header><div id="app">'+b+'</div></main>'}let q='';function render(){let list=items.filter(x=>(x.title+x.body+(x.meta?.tags||'')).toLowerCase().includes(q.toLowerCase()));shell('<section class="tool-layout"><aside class="panel"><h2>Bookmark</h2><input id="url" placeholder="https://example.com"><button id="fetch">Fetch title</button><input id="title" placeholder="Title"><input id="tags" placeholder="tags"><textarea id="body" placeholder="Description"></textarea><button id="save">Save bookmark</button></aside><section><input id="search" placeholder="Search" value="'+esc(q)+'"><div class="list">'+(list.map(bookmark).join('')||'<p class="muted">No bookmarks.</p>')+'</div></section></section>');$('#fetch').onclick=fetchTitle;$('#save').onclick=()=>addItem({title:$('#title').value,body:$('#body').value,status:'active',meta:{url:$('#url').value,tags:$('#tags').value}});$('#search').oninput=e=>{q=e.target.value;render()}}async function fetchTitle(){let r=await api('/api/page-title?url='+encodeURIComponent($('#url').value));$('#title').value=r.title}function bookmark(x){return '<article class="item-card"><h3><a href="'+esc(x.meta?.url)+'" target="_blank">'+esc(x.title)+'</a></h3><p>'+esc(x.body)+'</p><p class="muted">'+esc(x.meta?.tags||'')+'</p><button class="danger" onclick="deleteItem('+x.id+')">Delete</button></article>'}window.deleteItem=deleteItem;load();
